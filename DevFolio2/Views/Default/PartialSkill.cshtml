@model List<DevFolio2.Models.TblSkill>


<div class="skill-mf">
    <p class="title-s">Yeteneklerim</p>
    @foreach (var item in Model)
    {
<span>@item.SkillTitle</span> <span class="pull-right">@item.SkillValue%</span>
                <div class="progress">
                    <div class="progress-bar" role="progressbar" style="width: @item.SkillValue%;" aria-valuenow="@item.SkillValue" aria-valuemin="0"
                         aria-valuemax="100"></div>

                </div>}
</div>